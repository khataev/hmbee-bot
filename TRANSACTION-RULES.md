# Transaction Rules

Рабочая карта известных правил классификации транзакций.

Цель документа:
- держать перед глазами актуальные сценарии в human-readable виде;
- быстро сверять бизнес-правило с текущим `config/sources.json`;
- явно показывать расхождения между текущим runtime-конфигом и уже согласованным target-поведением из тестов/specs.

Источник истины на текущий момент:
- runtime-конфиг: `config/sources.json`
- реализация: `src/apply/preview/tochka.ts`, `src/apply/preview/ruleEngine.ts`
- тесты: `src/apply/preview-*.test.ts` (по одному файлу на type_code), `src/apply/preview.test.ts` (CardTransactionInfo + VedPaymentIncome)
- согласованный target для transfer-сценариев: `openspec/changes/preview-transfer-classification/specs/*`

Правила чтения таблиц:
- колонки `JSON logic эквивалент` и `JSON logic эквивалент (excluded)` показывают текущее выражение из `sources.json`;
- если сценарий уже согласован, но текущий `sources.json` ещё не полностью выражает его, это явно указано в колонке `Примечание`;
- `owned account` означает, что счёт найден в общем `accountRegistry` (реализовано custom-операцией `is_owned`);
- `deposit-like` означает счёт, начинающийся с `421` при BIC `044525104` (реализовано custom-операцией `is_deposit`).

Важно: `is_deposit` — это **строгое подмножество** `is_owned`. Функция `createAccountRegistry.isOwned` возвращает `true` для любого счёта с BIC `044525104`, начинающегося с `421`, без добавления его в `accountMappings`. Депозитные счета Точки автоматически открываются и закрываются, поэтому они не регистрируются в конфиге — ни в production, ни в тестах. Правило `NOT DEPOSIT_PAYER` в `excluded` нужно именно потому, что без него `is_owned(payer)` вернул бы `true` для депозитного счёта и deposit return был бы ошибочно excluded.

Сокращения в JSON logic:
- `OWNED_PAYER` = `{"is_owned":[{"var":"record.data.payerAccountId"},{"var":"record.data.payerBankBic"},{"var":"accountRegistry"}]}`
- `OWNED_PAYEE` = `{"is_owned":[{"var":"record.data.payeeAccountId"},{"var":"record.data.payeeBankBic"},{"var":"accountRegistry"}]}`
- `OWNED_RECIPIENT` = `{"is_owned":[{"var":"record.data.recipientAccountId"},null,{"var":"accountRegistry"}]}`
- `DEPOSIT_PAYER` = `{"is_deposit":[{"var":"record.data.payerAccountId"},{"var":"record.data.payerBankBic"}]}`
- `DEPOSIT_PAYEE` = `{"is_deposit":[{"var":"record.data.payeeAccountId"},{"var":"record.data.payeeBankBic"}]}`

## Точка

| Сценарий | type_code | identified | save | reason | Примечание | Условия отбора транзакции (included) | JSON logic эквивалент | Условия отбрасывания транзакции (excluded) | JSON logic эквивалент (excluded) | Ссылка на тест Кейс | Ссылка на фикстуру |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Карточная покупка | `CardTransactionInfo` | `true` | `true` | `null` | Обычный расход по карте. Для этого happy-path excluded-ветка не срабатывает, но в `sources.json` она есть. | `tranCode = Purchase` и `status in {InProgress, Withdraw}` | `{"or":[{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"InProgress"]}]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Withdraw"]}]}]}` | `tranCode = CheckCard` или `Purchase + status in {Canceled, Rejected}` | `{"or":[{"==":[{"var":"record.data.tranCode"},"CheckCard"]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Canceled"]}]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Rejected"]}]}]}` |  |  |
| Возврат по карте | `CardTransactionInfo` | `true` | `true` | `null` | Обычный доход по карте. Текущий runtime использует ту же excluded-ветку `CardTransactionInfo`, что и для карточных покупок. | `tranCode = ReverseByCard` и `status = Received` | `{"and":[{"==":[{"var":"record.data.tranCode"},"ReverseByCard"]},{"==":[{"var":"record.data.status"},"Received"]}]}` | `tranCode = CheckCard` или `Purchase + status in {Canceled, Rejected}` | `{"or":[{"==":[{"var":"record.data.tranCode"},"CheckCard"]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Canceled"]}]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Rejected"]}]}]}` |  |  |
| Проверка карты / отменённая или отклонённая карточная операция | `CardTransactionInfo` | `true` | `false` | `excluded` | Текущий `sources.json` явно помечает эти формы как excluded. | `—` | `—` | `tranCode = CheckCard` или `Purchase + status in {Canceled, Rejected}` | `{"or":[{"==":[{"var":"record.data.tranCode"},"CheckCard"]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Canceled"]}]},{"and":[{"==":[{"var":"record.data.tranCode"},"Purchase"]},{"==":[{"var":"record.data.status"},"Rejected"]}]}]}` |  |  |
| SBP C2B исходящий платёж | `SbpC2BPayment` | `true` | `true` | `null` | Обычный расход через SBP. Для happy-path excluded-ветка не срабатывает, но она есть и явно описана справа. | `status = ACCEPTED`, `incoming = false` | `{"and":[{"==":[{"var":"record.data.status"},"ACCEPTED"]},{"==":[{"var":"record.data.incoming"},false]}]}` | `status = CANCELED` или `status = REJECTED` или `incoming = true` | `{"or":[{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]}]},{"==":[{"var":"record.data.incoming"},true]}]}` | [src/apply/preview-SbpC2BPayment.test.ts#L48](src/apply/preview-SbpC2BPayment.test.ts#L48) | [src/apply/preview/fixtures/sbp-c2b-payment.json](src/apply/preview/fixtures/sbp-c2b-payment.json) |
| SBP C2B невалидный статус или входящая форма | `SbpC2BPayment` | `true` | `false` | `excluded` | Любая форма, которую текущий config считает не тем сценарием. Это точное содержание `excluded` в `sources.json` для `SbpC2BPayment`. | `—` | `—` | `status = CANCELED` или `status = REJECTED` или `incoming = true` | `{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]},{"==":[{"var":"record.data.incoming"},true]}]}` | [src/apply/preview-SbpC2BPayment.test.ts#L64](src/apply/preview-SbpC2BPayment.test.ts#L64) | [src/apply/preview/fixtures/sbp-c2b-payment-canceled.json](src/apply/preview/fixtures/sbp-c2b-payment-canceled.json), [src/apply/preview/fixtures/sbp-c2b-payment-rejected.json](src/apply/preview/fixtures/sbp-c2b-payment-rejected.json), [src/apply/preview/fixtures/sbp-c2b-payment-incoming.json](src/apply/preview/fixtures/sbp-c2b-payment-incoming.json) |
| SBP C2B возврат | `SbpC2BRefund` | `true` | `true` | `null` | Обычный доход через SBP refund. Для happy-path excluded-ветка не срабатывает, но она есть и совпадает с текущим runtime-конфигом. | `status = ACCEPTED`, `incoming = true` | `{"and":[{"==":[{"var":"record.data.status"},"ACCEPTED"]},{"==":[{"var":"record.data.incoming"},true]}]}` | `status in {CANCELED, REJECTED}` или `incoming = false` | `{"or":[{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]}]},{"==":[{"var":"record.data.incoming"},false]}]}` | [src/apply/preview-SbpC2BRefund.test.ts#L51](src/apply/preview-SbpC2BRefund.test.ts#L51) | [src/apply/preview/fixtures/sbp-c2b-refund.json](src/apply/preview/fixtures/sbp-c2b-refund.json) |
| SBP C2B refund в невалидной форме | `SbpC2BRefund` | `true` | `false` | `excluded` | Текущий config исключает отмены, отказы и исходящую форму. В sync есть только один happy-path пример, поэтому invalid-фикстуры синтетические на его основе с маскировкой. | `—` | `—` | `status in {CANCELED, REJECTED}` или `incoming = false` | `{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]},{"==":[{"var":"record.data.incoming"},false]}]}` | [src/apply/preview-SbpC2BRefund.test.ts#L64](src/apply/preview-SbpC2BRefund.test.ts#L64) | [src/apply/preview/fixtures/sbp-c2b-refund-canceled.json](src/apply/preview/fixtures/sbp-c2b-refund-canceled.json), [src/apply/preview/fixtures/sbp-c2b-refund-rejected.json](src/apply/preview/fixtures/sbp-c2b-refund-rejected.json), [src/apply/preview/fixtures/sbp-c2b-refund-outgoing.json](src/apply/preview/fixtures/sbp-c2b-refund-outgoing.json) |
| SBP B2C обычный исходящий платёж | `SbpB2CPayment` | `true` | `true` | `null` | Один и тот же broad include покрывает два сценария: перевод стороннему физлицу и перевод на свой внешний счёт. Для данного сценария это обычный расход (non-transfer). Excluded-ветка у типа тоже уже есть. | `status = ACCEPTED`, `incoming = false`, `payerAccountId is owned` | `{"and":[{"==":[{"var":"record.data.status"},"ACCEPTED"]},{"==":[{"var":"record.data.incoming"},false]},OWNED_PAYER]}` | `status in {CANCELED, REJECTED}` или `incoming = true` | `{"or":[{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]}]},{"==":[{"var":"record.data.incoming"},true]}]}` | [src/apply/preview-SbpB2CPayment.test.ts#L54](src/apply/preview-SbpB2CPayment.test.ts#L54) | [src/apply/preview/fixtures/sbp-b2c-payment-non-transfer.json](src/apply/preview/fixtures/sbp-b2c-payment-non-transfer.json) |
| SBP B2C перевод на мой внешний счёт | `SbpB2CPayment` | `true` | `true` | `null` | Тот же broad include, но в этом под-сценарии `payeeAccountId` принадлежит моему общему списку счетов, поэтому на этапе нормализации получается `normalized.type = transfer` и заполняется `counterpartyAccountId`. Разбивать include на две ветки не требуется. Excluded остаётся тем же, что у типа целиком. | `status = ACCEPTED`, `incoming = false`, `payerAccountId is owned` (+ сценарное различение через общий список моих счетов) | `Текущий sources.json: {"and":[{"==":[{"var":"record.data.status"},"ACCEPTED"]},{"==":[{"var":"record.data.incoming"},false]},OWNED_PAYER]}` | `status in {CANCELED, REJECTED}` или `incoming = true` | `{"or":[{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]}]},{"==":[{"var":"record.data.incoming"},true]}]}` | [src/apply/preview-SbpB2CPayment.test.ts#L69](src/apply/preview-SbpB2CPayment.test.ts#L69) | [src/apply/preview/fixtures/sbp-b2c-payment-own-transfer.json](src/apply/preview/fixtures/sbp-b2c-payment-own-transfer.json) |
| SBP B2C невалидный статус или входящая форма | `SbpB2CPayment` | `true` | `false` | `excluded` | Исключение по статусу или направлению. | `—` | `—` | `status in {CANCELED, REJECTED}` или `incoming = true` | `{"or":[{"or":[{"==":[{"var":"record.data.status"},"CANCELED"]},{"==":[{"var":"record.data.status"},"REJECTED"]}]},{"==":[{"var":"record.data.incoming"},true]}]}` | [src/apply/preview-SbpB2CPayment.test.ts#L84](src/apply/preview-SbpB2CPayment.test.ts#L84) | [src/apply/preview/fixtures/sbp-b2c-payment-canceled.json](src/apply/preview/fixtures/sbp-b2c-payment-canceled.json), [src/apply/preview/fixtures/sbp-b2c-payment-rejected.json](src/apply/preview/fixtures/sbp-b2c-payment-rejected.json), [src/apply/preview/fixtures/sbp-b2c-payment-incoming.json](src/apply/preview/fixtures/sbp-b2c-payment-incoming.json) |
| Комиссия | `PaymentWrittenOff` | `true` | `true` | `null` | Обычный расход. Эта ветка уже точно выражена в `sources.json`. | `incoming = false`, `objectState = Processed`, `failed = false`, `isComission = true` | `{"and":[{"==":[{"var":"record.data.incoming"},false]},{"==":[{"var":"record.data.objectState"},"Processed"]},{"==":[{"var":"record.data.failed"},false]},{"==":[{"var":"record.data.isComission"},true]}]}` | Пустая excluded-ветка. | `{"or":[]}` | [src/apply/preview-PaymentWrittenOff.test.ts#L67](src/apply/preview-PaymentWrittenOff.test.ts#L67) | [src/apply/preview/fixtures/payment-written-off-commission.json](src/apply/preview/fixtures/payment-written-off-commission.json) |
| Открытие депозита / исходящая canonical transfer-нога | `PaymentWrittenOff` | `true` | `true` | `null` | Target-сценарий: `normalized.type = transfer`, `counterpartyAccountId` обязателен. Текущий `sources.json` не знает deposit-like эвристику и вместо неё требует, чтобы и payer, и payee уже были owned в registry. | `incoming = false`, `isComission = false`, `payerAccountId is owned`, `payeeAccountId is deposit-like 421*` | `Ближайшая текущая ветка sources.json: {"and":[{"==":[{"var":"record.data.incoming"},false]},{"==":[{"var":"record.data.isComission"},false]},OWNED_PAYER,OWNED_PAYEE]}` | Пустая excluded-ветка. | `{"or":[]}` | [src/apply/preview-PaymentWrittenOff.test.ts#L80](src/apply/preview-PaymentWrittenOff.test.ts#L80) | [src/apply/preview/fixtures/payment-written-off-deposit-open-transfer.json](src/apply/preview/fixtures/payment-written-off-deposit-open-transfer.json) |
| Внутренний перевод между моими обычными счетами, исходящая нога | `PaymentAccepted` | `true` | `true` | `null` | Canonical transfer, `normalized.type = transfer`. `sources.json` включает обе ветки: transfer (payer + payee owned) и tax (isGovernment). | `incoming = false`, `isComission = false`, `payerAccountId is owned`, `payeeAccountId is owned` | `{"and":[{"==":[{"var":"record.data.incoming"},false]},{"==":[{"var":"record.data.isComission"},false]},OWNED_PAYER,OWNED_PAYEE]}` | Пустая excluded-ветка. | `{"or":[]}` | [src/apply/preview-PaymentAccepted.test.ts#L72](src/apply/preview-PaymentAccepted.test.ts#L72) | [src/apply/preview/fixtures/payment-accepted-internal-transfer.json](src/apply/preview/fixtures/payment-accepted-internal-transfer.json) |
| Внешний платёж в бюджет (налог) | `PaymentAccepted` | `true` | `true` | `null` | Обычный расход: оплата налогов и госпошлин. `payeeAccountId` не owned. Различение с transfer через `isGovernment = true`. | `incoming = false`, `isComission = false`, `isGovernment = true`, `payerAccountId is owned` | `{"and":[{"==":[{"var":"record.data.incoming"},false]},{"==":[{"var":"record.data.isComission"},false]},{"==":[{"var":"record.data.isGovernment"},true]},OWNED_PAYER]}` | Пустая excluded-ветка. | `{"or":[]}` | [src/apply/preview-PaymentAccepted.test.ts#L85](src/apply/preview-PaymentAccepted.test.ts#L85) | [src/apply/preview/fixtures/payment-accepted-tax.json](src/apply/preview/fixtures/payment-accepted-tax.json) |
| Внутренний перевод между моими обычными счетами, входящая зеркальная нога | `PaymentIncome` | `true` | `false` | `excluded` | Миррорная нога: payer owned, не deposit-like. `excluded`-ветка явно использует `is_deposit`-гард, чтобы не зацепить возврат тела депозита. | `—` | `—` | `incoming = true`, `payerAccountId is owned`, `NOT DEPOSIT_PAYER` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},OWNED_PAYER,{"!":DEPOSIT_PAYER}]}` | [src/apply/preview-PaymentIncome.test.ts#L79](src/apply/preview-PaymentIncome.test.ts#L79) | [src/apply/preview/fixtures/payment-income-mirrored.json](src/apply/preview/fixtures/payment-income-mirrored.json) |
| Возврат тела депозита | `PaymentIncome` | `true` | `true` | `null` | Canonical transfer, `normalized.type = transfer`, `counterpartyAccountId` обязателен. Ветка `included` Branch 1 явно проверяет `DEPOSIT_PAYER`. Депозитные счета в `accountMappings` не регистрируются (автооткрытие/автозакрытие), но `isOwned` возвращает `true` для них в рамках 421*-эвристики — поэтому deposit return корректно классифицируется как transfer. | `incoming = true`, `isComission = false`, `payeeAccountId is owned`, `DEPOSIT_PAYER` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},{"==":[{"var":"record.data.isComission"},false]},OWNED_PAYEE,DEPOSIT_PAYER]}` | `incoming = true`, `payerAccountId is owned`, `NOT DEPOSIT_PAYER` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},OWNED_PAYER,{"!":DEPOSIT_PAYER}]}` | [src/apply/preview-PaymentIncome.test.ts#L89](src/apply/preview-PaymentIncome.test.ts#L89) | [src/apply/preview/fixtures/payment-income-deposit-return.json](src/apply/preview/fixtures/payment-income-deposit-return.json) |
| Проценты по депозиту | `PaymentIncome` | `true` | `true` | `null` | Ветка `included` Branch 2: payer не owned, BIC Точки (`044525104`). Payer — внутренний банковский счёт (например, `474*`), не являющийся owned и не deposit-like. | `incoming = true`, `isComission = false`, `payeeAccountId is owned`, `NOT OWNED_PAYER`, `payerBankBic = 044525104` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},{"==":[{"var":"record.data.isComission"},false]},OWNED_PAYEE,{"!":OWNED_PAYER},{"==":[{"var":"record.data.payerBankBic"},"044525104"]}]}` | `incoming = true`, `payerAccountId is owned`, `NOT DEPOSIT_PAYER` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},OWNED_PAYER,{"!":DEPOSIT_PAYER}]}` | [src/apply/preview-PaymentIncome.test.ts#L101](src/apply/preview-PaymentIncome.test.ts#L101) | [src/apply/preview/fixtures/payment-income-deposit-interest.json](src/apply/preview/fixtures/payment-income-deposit-interest.json) |
| Внешний входящий платёж | `PaymentIncome` | `true` | `true` | `null` | Ветка `included` Branch 3: payer не owned, BIC не Точки. Отдельная ветка делает явным, что внешние платежи явно ожидаются и не являются «пропущенными» случаями Branch 2. | `incoming = true`, `isComission = false`, `payeeAccountId is owned`, `NOT OWNED_PAYER`, `payerBankBic ≠ 044525104` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},{"==":[{"var":"record.data.isComission"},false]},OWNED_PAYEE,{"!":OWNED_PAYER},{"!":{"==":[{"var":"record.data.payerBankBic"},"044525104"]}}]}` | `incoming = true`, `payerAccountId is owned`, `NOT DEPOSIT_PAYER` | `{"and":[{"==":[{"var":"record.data.incoming"},true]},OWNED_PAYER,{"!":DEPOSIT_PAYER}]}` | [src/apply/preview-PaymentIncome.test.ts#L113](src/apply/preview-PaymentIncome.test.ts#L113) | [src/apply/preview/fixtures/payment-income-external.json](src/apply/preview/fixtures/payment-income-external.json) |
| Недистрибьютированный входящий ВЭД-платёж | `VedPaymentIncome` | `true` | `true` | `null` | Обычный доходный сценарий для arrival/VED. | `state = UNDISTRIBUTED`, `recipientAccountId is owned` | `{"and":[{"==":[{"var":"record.data.state"},"UNDISTRIBUTED"]},OWNED_RECIPIENT]}` | Пустая excluded-ветка. | `{"or":[]}` |  |  |
| Любая известная форма без совпадения по `included` и `excluded` | `Любой поддерживаемый type_code` | `false` | `false` | `no matching included/excluded condition` | Общий fallback preview-классификатора. Полезно держать в голове как baseline для новых кейсов. | Не выполнилось ни одно условие `included`. | `—` | Не выполнилось ни одно условие `excluded`. | `—` |  |  |
| Неподдерживаемый `type_code` | `Любой неизвестный type_code` | `false` | `false` | `unsupported type_code: <value>` | Общий fallback для неподлежащих нормализации записей. | `—` | `—` | `—` | `—` |  |  |

## Что сейчас особенно важно помнить

- `PaymentIncome` — три явные OR-ветки в `included` (deposit return, deposit interest, external). `excluded` явно использует `NOT DEPOSIT_PAYER`, чтобы не захватить возврат тела депозита. Источник истины — `sources.json` и `preview-PaymentIncome.test.ts`.
- `PaymentWrittenOff` в текущем `sources.json` выражает deposit opening через `OWNED_PAYEE` (депозитный счёт ещё не в реестре, поэтому technically это `payer_owned AND payee_not_owned`, что работает случайно). Target-поведение задокументировано в тестах и specs.
- `SbpB2CPayment` в текущем `sources.json` описан одним broad include (`OWNED_PAYER`), а различение между «сторонний физлицо» и «мой внешний счёт» делается по общему списку моих счетов на этапе нормализации и в тестах.

## Как поддерживать документ дальше

- При изменении business-правила сначала обновлять эту карту, потом `sources.json`, потом `sources.json.example`, потом тесты.
- Если один `type_code` покрывает несколько разных бизнес-сценариев, держать отдельные строки на каждый сценарий и отдельные OR-ветки в `sources.json`.